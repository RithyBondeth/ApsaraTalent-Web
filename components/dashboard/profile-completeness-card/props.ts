import { IProfileCompletionResult } from "@/utils/interfaces/user/profile-completion.interface";

export interface IProfileCompletenessCardProps {
  completion: IProfileCompletionResult;
  profileUrl: string;
}
